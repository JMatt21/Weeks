<!-- 5. Output the animalType string, pet boolean, and fierceness number to the page for all the pets. -->
<h2>
    <ul>
        {{#each arr }} 
        {{ animalType }}
        {{ pet }}
        {{ fierceness }}
        <br> 
        {{/each }}
    </ul>
</h2>